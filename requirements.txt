# =============================================================
#  VitalTracker — Python Dependencies
#
#  Install:
#    pip install -r requirements.txt
#
#  NOTE: The edge layer (ESP32 firmware) has its own library
#  dependencies managed through the Arduino IDE Library Manager.
#  See edge/patient_monitor/README.md for details.
# =============================================================

# --- Fog Layer: MQTT Ingestion ---
paho-mqtt>=2.0,<3.0

# --- Fog Layer: Dashboard ---
streamlit>=1.30,<2.0
streamlit-autorefresh>=1.0,<2.0
plotly>=5.18,<6.0

# --- Fog Layer: Model Inference & Anomaly Detection ---
tensorflow>=2.15,<3.0
numpy>=1.26,<2.0
scikit-learn>=1.4,<2.0

# --- Notebooks: EDA, Preprocessing, Training, Evaluation ---
pandas>=2.1,<3.0
matplotlib>=3.8,<4.0
seaborn>=0.13,<1.0
jupyter>=1.0,<2.0
